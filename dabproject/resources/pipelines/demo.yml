resources:
  pipelines:
    pipeline_bank_demo_pipeline:
      name: bank_demo_pipeline
      libraries:
        - glob:
            include: ../../src/pipelines/bank_demo_pipeline/transformations/**
      schema: bronze
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog_name}
      serverless: true
      root_path: ../../src/pipelines/bank_demo_pipeline
